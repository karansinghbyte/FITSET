# FITSET
User visits `index.html` (landing page) User clicks "Start Free Trial" → redirects to `auth.html` User registers/logs in User accesses personalized dashboard Backend generates AI recommendations via Gemini API User tracks daily activities System monitors progress over time
